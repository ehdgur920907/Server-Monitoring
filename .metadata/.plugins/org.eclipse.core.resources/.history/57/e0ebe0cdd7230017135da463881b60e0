<?xml version="1.0" encoding="UTF-8"?>  
	<!DOCTYPE mapper 
	PUBLIC "-//ibatis.apache.org//DTD Mapper 3.0//EN" 
	"http://ibatis.apache.org/dtd/ibatis-3-mapper.dtd">  
  
    <mapper namespace="MemoryMapper">
    	
    	<select id="selectMemorys" parameterType="string" resultMap="HashMap">
    		SELECT * FROM memory WHERE used_memory = #{used_memory}
    	</select>
    	
    	<insert id="insertMemory" parameterType="java.util.HashMap">
    		INSERT INTO memory (used_memory as usedMemory, total_memory as totalMemory, free_memory as freeMemory)
    			VALUSE(#{usedMemory},#{totalMemory},#{freeMemory})
    	</insert>
    	
    	<delete id="deleteMemory" parameterType="java.util.HashMap">
    		DELETE FROM memory WHERE usedMemory as used_memory = #{usedMemory}
    	</delete>
    </mapper>